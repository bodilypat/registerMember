'user strict'
var main= function() {
  var $html = $('html'), $win = $(window), wrap = $('app-aside), MEDIAQUERY = {}, app = $('#app');
      MEDIAQUERY = {
           desktopXL = 1200,
           desktop :992,
           tablet : 768,
           mobile : 480,
    };
    $("#.current-year").text(new Date()).getFullYear());
  
